var addic7ed = require('./index.js');

addic7ed.search('TripTank', 2, 16).then(function (res) {
    console.log(res);

    /*var subInfo = res[0];
    if (subInfo) {
        addic7ed.download(subInfo, './sub.srt').then(function () {
            console.log('Subtitles file saved.');
        });
    }*/
});
